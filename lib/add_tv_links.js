var cheerio = require('cheerio');
var async = require('async');

function add_tv_links(tv, $, callback) {

    // Need to get the current docs from the DB,
    // to know what has been removed




    callback(null, 'OK');
}

module.exports = add_tv_links;
